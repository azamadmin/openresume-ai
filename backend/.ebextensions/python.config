option_settings:
  aws:elasticbeanstalk:container:python:
    WSGIPath: wsgi.py   # Points to the wsgi.py file we just created

container_commands:
  01_mkdir_uploads:
    command: "mkdir -p backend/uploads"
